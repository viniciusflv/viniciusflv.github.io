<section class="hero">
  <div class="hero__content">
    <header class="hero__header">
      <slot />
    </header>
    <slot name="image" />
  </div>
</section>

<style is:global>
  .enabled {
    .hero {
      @apply shadow-lg
      text-gray-400
      bg-gray-200
      dark:bg-dark-background
      glow-b-dark-comment
      dark:glow-b-dark-green;

      .hero__content {
        @apply container
        p-8
        py-32
        xl:p-32
        mx-auto
        grid
        items-center
        md:grid-cols-2;

        .hero__header {
          @apply flex
          flex-col
          gap-8;

          h1 {
            @apply text-2xl
            md:text-3xl
            lg:text-5xl
            font-bold
            tracking-wider;

            @apply text-transparent
            bg-clip-text
            bg-gradient-to-r
            from-dark-currentLine
            to-dark-comment
            dark:from-dark-cyan 
            dark:to-dark-green;
          }
        }
      }
    }
  }
</style>
