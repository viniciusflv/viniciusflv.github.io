---
layout: '../../layouts/Layout.astro'
title: 'Blog'
---

import Mermaid from '../../layouts/Mermaid.astro';

# Blog

```ts
const x: string = 'a';
```

<Mermaid
  chart={`graph LR
    A[Client] ---->|tcp_123| B
    B((Load Balancer))
    B ---->|tcp_456| C[Server1]
    B ---->|tcp_456| D[Server2]
  `}
/>

<Mermaid
  chart={`graph LR
    A[Client] ---->|tcp_123| B
    B((Load Balancer))
    B ---->|tcp_456| C[Server1]
    B ---->|tcp_456| D[Server2]
  `}
/>
